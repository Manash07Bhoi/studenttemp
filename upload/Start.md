# 📄 FILE: `GO-NO-GO-REVIEW.md` — Final Pre-Build Verification

I did a hard second pass, specifically checking: *"Are the CRITICAL items from `GAP-ANALYSIS.md` actually closed, or just identified?"*

**Honest finding: they were only identified, not closed.** Listing a gap is not the same as resolving it. Before I tell you "go build," I'm going to actually close the 6 CRITICAL blockers right now with firm decisions — because they were architectural *decisions*, not large documents, and leaving them open would mean your coding agent hits ambiguity on Day 1 and guesses wrong.

---

## Step 1 — Closing the 6 CRITICAL Blockers (Final Decisions, Locked)

### ✅ C1 — Inbound SMTP path: **DECIDED**
Postfix (self-hosted on Oracle Cloud Always-Free VM) is the mail path. Postfix does recipient validation via a lookup table/socket call to your Go Mail Gateway (`check_recipient_access` via `tcp_table` or a Postfix policy service) before accepting DATA. Cloudflare is DNS/WAF/edge for web traffic only — **not** in the mail path. This closes the contradiction.

### ✅ C2 — Backscatter/bounce policy: **DECIDED**
Reject unknown/expired/over-quota recipients at `RCPT TO` with a hard SMTP `550 5.1.1` — inside the SMTP conversation, before DATA is ever accepted. **Never** accept-then-bounce. No DSN is ever generated by StudentTemp. This is the single most important anti-backscatter rule in the whole system — flag it as non-negotiable in `AGENT.md`.

### ✅ C3 — API contract: **DECIDED (format), file still needs writing**
Format = OpenAPI 3.1, single source of truth at `/api-spec/openapi.yaml`, generated TypeScript types consumed by both frontend and BFF via codegen (e.g., `openapi-typescript`). **This file must be written before any endpoint code is written** — it's a Phase 0 deliverable, not optional groundwork.

### ✅ C4 — Session transport: **DECIDED**
`HttpOnly` + `Secure` + `SameSite=Strict` cookie for the session; CSRF double-submit token on all mutating requests; the human-readable recovery code (`ST-7X9K-2Q4M`) is separate and only used to re-mint a cookie on a new device via a dedicated `/session/recover` endpoint.

### ✅ C5 — Malware scanner: **DECIDED**
ClamAV (`clamd`), self-hosted alongside the mail processor, free, real, no fabricated results possible.

### ✅ C6 — Challenge provider: **DECIDED**
Cloudflare Turnstile, free tier, no third-party tracking conflict with the privacy positioning.

**Status: all 6 CRITICAL architectural ambiguities are now resolved decisions. Nothing is left for the coding agent to guess on the mail-path, auth-transport, or anti-abuse stack.**

---

## Step 2 — Re-check HIGH items: are any of them *actually* blocking to start, or safe to resolve during Phase 0/1?

| Item | Blocking to start? | Verdict |
|---|---|---|
| H1 Entropy spec | No — small decision, already given (10-char CSPRNG local-part, 21-char nanoid IDs) | Resolved inline, non-blocking |
| H2 Alias race condition | No — decision given (Redis `SETNX` lock + Postgres unique constraint) | Resolved inline, non-blocking |
| H3 SSE reconnect logic | No — can be implemented during Phase 2, doesn't block Phase 0/1 scaffolding | Non-blocking |
| H4 Multi-tab sync | No — Phase 2 polish item | Non-blocking |
| H5 Homograph spoofing UI | **Should be added before Message Reader ships**, but doesn't block Phase 0 setup | Blocking for **Phase 1 Screen 5 only**, not for starting the project |
| H6 Default quotas | No — table already provided above, can go straight into config | Resolved inline, non-blocking |
| H7 Free-tier capacity ceilings | No — monitor as you go, not a build blocker | Non-blocking |
| H8 Search engine choice | No — Phase 3 feature, Postgres FTS decided | Non-blocking |
| H9 DPDP compliance | **Should not launch publicly without this**, but does not block writing code | Blocking for **public launch**, not for starting the build |
| H10 Incident response runbook | Same as H9 — blocks launch, not coding | Blocking for **launch**, not for starting |

**Conclusion: none of the HIGH items block you from starting Phase 0 engineering work today.** H5 must be done before Screen 5 ships in Phase 1. H9/H10 must be done before public production launch — not before writing code.

---

## Step 3 — Final Go/No-Go Decision, by Phase

| Phase | Status | Condition |
|---|---|---|
| **Phase 0 — Foundations** (repo scaffold, design tokens, CI skeleton, DNS, schema migrations, security headers, legal page drafts) | 🟢 **GO — start now** | None. All decisions needed for this phase are locked. |
| **Phase 1 — MVP core** (SMTP receive, inbox, reader, expiration, cleanup, basic rate limiting) | 🟢 **GO, with one embedded task**: write `openapi.yaml` (C3) and the homograph-spoofing UI rule (H5) as part of this phase, not after | Do not skip the OpenAPI contract — write it before endpoint code. |
| **Phase 2 — Real-time & personalization** (custom alias, Indian domain pack, SSE, PWA, notifications) | 🟢 GO once Phase 1 passes its own Definition of Done | No new blockers found |
| **Phase 3 — Developer & scale / Verified Institution Partner Program** | 🟡 Conditional — requires legal review of the DNS-verified institution-partner model before contracts are signed with any real school | Not a coding blocker, a legal/business blocker |
| **Public Production Launch (any phase)** | 🔴 **NOT yet** — requires H9 (DPDP notice + data-principal request flow) and H10 (real incident response runbook) to exist first | These are launch gates, already listed correctly in `SOP.md`'s pre-release checklist — just make sure nobody skips them under deadline pressure |

---

## ✅ Final Confirmation

**Yes — you can start building now**, specifically:

1. Begin **Phase 0** immediately using the documentation set as-is — no remaining ambiguity blocks scaffolding, CI, schema, or design-system work.
2. When you reach **Phase 1's mail-core work**, hand the AI coding agent this updated decision set (Postfix path, RCPT-TO rejection rule, ClamAV, Turnstile, cookie-based session, entropy spec) *in addition to* the original `AGENT.md` — these are now non-negotiable implementation constraints, not open questions.
3. Do **not** flip the switch to public production traffic until H9 (DPDP notice/consent + data request flow) and H10 (incident response runbook) are actually written — that remains the one genuine hold point, and it's a launch gate, not a coding gate.
4. Track H5 (sender spoofing UI) as a hard requirement of the Message Reader screen's Definition of Done in Phase 1, since it's a real security-relevant UX element, not cosmetic polish.

**Net result:** all previously-identified critical ambiguity is now resolved with concrete, locked decisions. The only remaining open items are pre-launch legal/compliance gates (expected and normal for any product handling EU/India-style privacy obligations) — not engineering blockers. You have a buildable, internally consistent, non-contradictory specification.
